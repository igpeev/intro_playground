# Virtual Environment = light-weight, self-contained Python installation

# Create (-m) virtual environment (venv) in a folder (venv3)
# $ python3 -m venv venv3

# Activate(on Windows just run it in cmd, prompt should chagnge to (venv3) $
# $ venv3\bin\activate

# Deactivate (leave)
# (venv3) $ deactivate

# To run or verify whether it's installed
# $ python3 -m venv
